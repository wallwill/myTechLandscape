node_modules/
package-lock.json

# SQLite artifacts (migrated to PostgreSQL)
data/*.db
data/*.db-shm
data/*.db-wal

# Cached/generated data files
data/landscape_cache.json
data/assignments.json

# Keep landscape.yml (bundled data)
!data/landscape.yml

# Environment
.env
.env.local
